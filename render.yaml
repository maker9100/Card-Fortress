services:
  - type: web
    name: card-fortress-moderation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn moderation-server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
